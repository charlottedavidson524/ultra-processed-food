[
    project
]
name = "food-classifier"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "duckdb",
    "matplotlib",
    "seaborn",
    "jupyter"
]

[
    project.optional-dependencies
]
dev = ["pytest", "ruff"]